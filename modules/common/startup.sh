# ============================================================================
# Startup Commands
# ============================================================================
# Commands to run when a new shell session starts

# Display system info
if command -v hyfetch &> /dev/null; then
    hyfetch
fi
